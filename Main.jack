/** Main program to display only the Grid. */
class Main {
    function void main() {
        var Grid grid; // Declare a Grid object

        // Create a 3x3 grid centered on the screen with cell size 50
        let grid = Grid.new(50);
        do grid.draw();

        return;
    }
}
