/** Main program to display the StartScreen. */
class Main {
    function void main() {
        // Directly call StartScreen.display as a class-level function
        do StartScreen.display();

        return;
    }
}
