class Main {
    function void main() {
        var Game game;

        // Initialize the game
        let game = Game.new();

        // Simulate two player turns
        do game.playerTurn(88); // Player X takes a turn


        return;
    }
}
